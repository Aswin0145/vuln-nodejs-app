# This is a basic workflow to help you get started with boman.ai

name: Bomanai Scanner aswinprof prod
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Boman-scan:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Install packages
        run: sudo pip install boman-cli
      - name: Run scan
        run: sudo boman-cli -a run -at 5196ed91-0ee9-4669-951d-7c493c24928b -ct 41bb9111-e57f-43d9-aa15-8c2e751ddb36
